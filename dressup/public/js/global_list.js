// "Created By" (owner) field for all DocType list views.
// NOTE: `owner` is already included in frappe.model.std_fields_list,
// so it is fetched automatically by every list view. No override needed.
// The visible column injection is handled by global_owner_column.js.
